spring.application.name=omong

# ===== 업로드(키오스크 사진) =====
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB

# ===== AI 제공자 스위치 =====
# ollama | mock  (ollama가 안 떠 있으면 자동으로 mock 폴백)
kiosk.ai.provider=ollama

# ===== Ollama (로컬) =====
spring.ai.ollama.base-url=http://localhost:11434
# 멀티모달(사진 읽기) 가능한 모델 권장: llava / llama3.2-vision / qwen2.5vl
spring.ai.ollama.chat.options.model=llava
spring.ai.ollama.chat.options.temperature=0.2
# 모델 없으면 자동 다운로드하려면 아래 주석 해제 (용량 큼)
# spring.ai.ollama.init.pull-model-strategy=when_missing

# 비전 전용 모델을 따로 쓰고 싶을 때 (없으면 위 chat 모델 사용)
kiosk.ai.vision-model=llava

# ===== [추후] OpenAI =====
# spring.ai.openai.api-key=${OPENAI_API_KEY}
# spring.ai.openai.chat.options.model=gpt-4o

# ===== [추후] Claude (Anthropic) =====
# spring.ai.anthropic.api-key=${ANTHROPIC_API_KEY}
# spring.ai.anthropic.chat.options.model=claude-sonnet-4-6

# ===== [추후] MariaDB 벡터 스토어 (RAG) =====
# spring.datasource.url=jdbc:mariadb://localhost:3306/omong
# spring.datasource.username=omong
# spring.datasource.password=${DB_PASSWORD}
